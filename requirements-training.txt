# LoRA Fine-Tuning Dependencies (GPU only)
# Install: pip install -r requirements-training.txt

# Core ML frameworks
torch>=2.1.0
transformers>=4.36.0
accelerate>=0.25.0

# LoRA and quantization
peft>=0.7.0
bitsandbytes>=0.41.0
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git

# Training utilities
trl>=0.7.0
datasets>=2.15.0

# Model export
gguf>=0.1.0
